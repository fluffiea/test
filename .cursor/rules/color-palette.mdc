---
description: 项目色系规范（自然系五色调色板）。编写或审查 apps/mobile 中的界面样式、组件配色时强制执行，确保所有用色取自本色板，不引入额外颜色。
globs:
  - "apps/mobile/**/*.css"
  - "apps/mobile/**/*.scss"
  - "apps/mobile/**/*.tsx"
  - "apps/mobile/**/*.ts"
alwaysApply: false
---

# 项目色系规范

本项目**只使用以下五个颜色**，不引入其他色彩：

| Token           | Hex       | 定位                    |
|-----------------|-----------|-------------------------|
| `rose-pink`     | `#D6A2AD` | 主强调色 / 品牌色 / CTA  |
| `warm-sand`     | `#C3B59F` | 中性过渡 / 辅助背景      |
| `sage-green`    | `#A0AF84` | 次强调色 / 成功状态      |
| `teal-green`    | `#668F80` | 交互元素 / 图标 / 链接   |
| `deep-slate`    | `#4A6670` | 正文 / 标题 / 深色背景   |

## Tailwind 用法

颜色已在 `tailwind.config.js` 的 `theme.extend.colors` 中注册，直接使用原子类：

```tsx
// ✅ 正确
<View className="bg-rose-pink text-deep-slate border-teal-green" />

// ❌ 错误：不使用色板外的颜色
<View className="bg-blue-500 text-gray-900" />
```

## CSS 变量（自定义类中引用）

```css
/* ✅ 正确 */
.my-card { background: var(--color-warm-sand); color: var(--color-deep-slate); }

/* ❌ 错误：硬编码色板外的颜色 */
.my-card { background: #ffffff; }
```

## 场景速查

- **主按钮 / CTA**：`rose-pink` 背景 + 白色文字
- **页面底色**：`warm-sand`（或白色做叠加底）
- **次要按钮 / 标签**：`sage-green`
- **链接 / 图标**：`teal-green`
- **正文 / 标题**：`deep-slate`
- **分隔线 / 边框**：`warm-sand` 或 `teal-green`（可加透明度，如 `/40`）

## 禁止项

- ❌ 不使用纯黑 `#000` / 纯白 `#fff` 作为主色（叠加层可用）
- ❌ 不硬编码色板外的 hex 值
- ❌ 需要深浅层次时用同色透明度（如 `bg-rose-pink/20`），不新增颜色
